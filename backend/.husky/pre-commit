#!/bin/sh
if git diff --cached --name-only | xargs grep -lE "(MONGO_URI|JWT_SECRET|GEMINI_API_KEY)\s*=\s*['\"a-zA-Z0-9]" 2>/dev/null | grep -v ".env.example"; then
  echo "❌ Possible secret detected in staged files. Aborting commit."
  exit 1
fi